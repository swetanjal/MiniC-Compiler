#include "bits/stdc++.h"
using namespace std;

class ASTProg;

class ASTnode
{
public:
    virtual ~ASTnode()
    {
    }
    //  virtual void printPostFix() const = 0;
    // virtual void accept(ASTvisitor &V) = 0;
};